<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    public function up(): void
    {
        DB::unprepared(<<<'SQL'
CREATE TABLE "PAYMENTS"
   (	"PAYMENT_NO" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 48 CACHE 20 NOORDER  NOCYCLE  NOT NULL ENABLE,
	"INVOICE_NO" NUMBER NOT NULL ENABLE,
	"PAYMENT_METHOD" VARCHAR2(50),
	"AMOUNT" NUMBER(10,2) NOT NULL ENABLE,
	"CURRENCY_NO" NUMBER NOT NULL ENABLE,
	"USD" NUMBER(10,2) NOT NULL ENABLE,
	"CREATE_AT" TIMESTAMP (6) DEFAULT sysdate NOT NULL ENABLE,
	"UPDATE_AT" TIMESTAMP (6) DEFAULT sysdate NOT NULL ENABLE,
	"RECIEVE_AMOUNT" NUMBER(10,2) NOT NULL ENABLE,
	"DEBT_AMOUNT" NUMBER(10,2) NOT NULL ENABLE,
	 CONSTRAINT "PAYMENTS_CHK1" CHECK (debt_amount<amount) ENABLE,
	 CONSTRAINT "PAYMENTS_PK" PRIMARY KEY ("PAYMENT_NO")
  USING INDEX  ENABLE,
	 CONSTRAINT "PAYMENTS_FK1" FOREIGN KEY ("CURRENCY_NO")
	  REFERENCES "CURRENCIES" ("CURRENCY_NO") ENABLE,
	 CONSTRAINT "PAYMENTS_FK2" FOREIGN KEY ("INVOICE_NO")
	  REFERENCES "INVOICES" ("INVOICE_NO") ENABLE
   )
SQL
        );
    }

    public function down(): void
    {
        DB::unprepared('DROP TABLE "PAYMENTS" CASCADE CONSTRAINTS PURGE');
    }
};
