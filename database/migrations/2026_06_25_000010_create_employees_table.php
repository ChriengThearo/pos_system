<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    public function up(): void
    {
        DB::unprepared(<<<'SQL'
CREATE TABLE "EMPLOYEES"
   (	"EMPLOYEE_ID" NUMBER(6,0) GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 154 CACHE 20 NOORDER  NOCYCLE  NOT NULL ENABLE,
	"EMPLOYEE_NAME" VARCHAR2(50) NOT NULL ENABLE,
	"GENDER" VARCHAR2(6),
	"BIRTH_DATE" DATE,
	"JOB_ID" NUMBER(3,0),
	"ADDRESS" VARCHAR2(150),
	"PHONE" VARCHAR2(15),
	"SALARY" NUMBER(8,0),
	"REMARKS" VARCHAR2(50),
	"PHOTO" BLOB,
	 CONSTRAINT "UQ_EMPLOYEES_PHONE" UNIQUE ("PHONE")
  USING INDEX  ENABLE,
	 CONSTRAINT "PK_EMPLOYEES" PRIMARY KEY ("EMPLOYEE_ID")
  USING INDEX  ENABLE,
	 CONSTRAINT "FK_EMPLOYEES_JOB" FOREIGN KEY ("JOB_ID")
	  REFERENCES "JOBS" ("JOB_ID") ENABLE
   )
SQL
        );
    }

    public function down(): void
    {
        DB::unprepared('DROP TABLE "EMPLOYEES" CASCADE CONSTRAINTS PURGE');
    }
};
