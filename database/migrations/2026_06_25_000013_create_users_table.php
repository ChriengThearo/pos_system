<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    public function up(): void
    {
        DB::unprepared(<<<'SQL'
CREATE TABLE "USERS"
   (	"USER_ID" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 26 CACHE 20 NOORDER  NOCYCLE  NOT NULL ENABLE,
	"E_ID" NUMBER NOT NULL ENABLE,
	"G_ID" NUMBER NOT NULL ENABLE,
	"PASSWORD" VARCHAR2(20),
	"CREATE_DATE" DATE DEFAULT sysdate,
	"STATUS" VARCHAR2(20),
	 CONSTRAINT "USERS_PK" PRIMARY KEY ("G_ID", "E_ID")
  USING INDEX  ENABLE,
	 CONSTRAINT "USERS_FK1" FOREIGN KEY ("E_ID")
	  REFERENCES "EMPLOYEES" ("EMPLOYEE_ID") ENABLE,
	 CONSTRAINT "USERS_FK2" FOREIGN KEY ("G_ID")
	  REFERENCES "GROUP_USER" ("G_ID") ENABLE
   )
SQL
        );
    }

    public function down(): void
    {
        DB::unprepared('DROP TABLE "USERS" CASCADE CONSTRAINTS PURGE');
    }
};
