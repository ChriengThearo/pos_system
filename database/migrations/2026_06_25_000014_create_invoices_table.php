<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    public function up(): void
    {
        DB::unprepared(<<<'SQL'
CREATE TABLE "INVOICES"
   (	"INVOICE_NO" NUMBER(12,0) GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1946 CACHE 20 NOORDER  NOCYCLE  NOT NULL ENABLE,
	"INVOICE_DATE" DATE DEFAULT SYSDATE,
	"CLIENT_NO" NUMBER(8,0) NOT NULL ENABLE,
	"EMPLOYEE_ID" NUMBER(6,0) NOT NULL ENABLE,
	"INVOICE_STATUS" VARCHAR2(30),
	"INVOICE_MEMO" VARCHAR2(100),
	 CONSTRAINT "PK_INVOICES" PRIMARY KEY ("INVOICE_NO")
  USING INDEX  ENABLE,
	 CONSTRAINT "FK_INVOICES_CLIENT" FOREIGN KEY ("CLIENT_NO")
	  REFERENCES "CLIENTS" ("CLIENT_NO") ENABLE,
	 CONSTRAINT "FK_INVOICES_EMPLOYEE" FOREIGN KEY ("EMPLOYEE_ID")
	  REFERENCES "EMPLOYEES" ("EMPLOYEE_ID") ENABLE
   )
SQL
        );
    }

    public function down(): void
    {
        DB::unprepared('DROP TABLE "INVOICES" CASCADE CONSTRAINTS PURGE');
    }
};
