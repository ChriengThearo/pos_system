<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    public function up(): void
    {
        DB::unprepared(<<<'SQL'
CREATE TABLE "PURCHASES"
   (	"PURCHASE_NO" NUMBER(3,0) GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 148 CACHE 20 NOORDER  NOCYCLE  NOT NULL ENABLE,
	"PURCHASE_DATE" DATE DEFAULT sysdate,
	"SUPLIER_ID" NUMBER(3,0),
	"EMPLOYEE_ID" NUMBER(3,0),
	"PURCHASE_MEMO" VARCHAR2(20),
	 CONSTRAINT "PURCHASES_PK" PRIMARY KEY ("PURCHASE_NO")
  USING INDEX  ENABLE,
	 CONSTRAINT "PURCHASES_FK1" FOREIGN KEY ("SUPLIER_ID")
	  REFERENCES "SUPPLIERS" ("SUPPLIER_ID") ENABLE,
	 CONSTRAINT "PURCHASES_FK2" FOREIGN KEY ("EMPLOYEE_ID")
	  REFERENCES "EMPLOYEES" ("EMPLOYEE_ID") ENABLE
   )
SQL
        );
    }

    public function down(): void
    {
        DB::unprepared('DROP TABLE "PURCHASES" CASCADE CONSTRAINTS PURGE');
    }
};
